<div id= "">
  <p style="color: green"><%= notice %></p>

  <div class= "header">
    <%= link_to groups_path do %><%= image_tag "back.png", class:"back"%><%end%>
    <h2>Transactions</h2>
    <%= button_to destroy_user_session_path, method: :delete, class: "off" do %> 
      <%= image_tag "off.png", class:"off"%>
    <%end%>
  </div>

  <div class= "total-transactions">
    <small class= "total-transactions">
    <strong>Total Transactions:</strong>
    $<%= @operations.inject(0) { |sum, t| sum += t.amount  }%> 
    </small>
  </div>

  <div id="operations">
    <% if @operations.any? %>
      <% @operations.each do |operation| %>
        <%= render operation %> 
      <% end %>
    <% else %>
      <p>No Transactions found, Add a new operation.</p>
    <% end %>
  </div>
  <div class= "new-group">
    <%= link_to "New operation", new_group_operation_path(@operations) %>
  </div>
</div>
